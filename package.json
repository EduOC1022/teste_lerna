{
  "name": "teste_lerna",
  "packageManager": "npm@10.2.3",
  "devDependencies": {
    "lerna": "8.1.3"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "lerna run rm && npx nx reset",
    "dev": "lerna run build --parallel=false && lerna run dev"
  }
}
